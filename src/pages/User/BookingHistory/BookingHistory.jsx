import Typography from "@mui/material/Typography";
import Box from "@mui/material/Box";

function BookingHistory() {
    return (
        <Box>
            <Typography>This is booking history page</Typography>
        </Box>
    );
}

export default BookingHistory;
